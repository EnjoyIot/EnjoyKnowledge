bin.name = "enjoyknowledge"
args = ["workflow", "capture", "--kind", "gotcha", "--field", "trigger=Spring Boot @Transactional", "--body", "TX transaction rollback issue"]
fs.cwd = "../fixtures/minimal-project"
fs.sandbox = true
